export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function MerchantCreateShipmentPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Create Shipment</h1>

      <p>
        Create Shipment page is temporarily disabled for production deployment.
      </p>

      <p>
        Backend and frontend deployment can continue now. The map-based shipment form will be fixed after production is live.
      </p>

      <a href="/merchant">
        Back to Merchant Dashboard
      </a>
    </main>
  );
}
