'use client';
import { useState } from 'react';
import api from '@/lib/api';
import { Button, Form, Input, Modal, SimpleTablePage, StatusTag, message } from '@/components/PageTools';
export default function MerchantPickupsPage(){const[open,setOpen]=useState(false);const[refresh,setRefresh]=useState(0);const[form]=Form.useForm();const columns=[{title:'Name',dataIndex:'pickup_name'},{title:'Phone',dataIndex:'pickup_phone'},{title:'Address',dataIndex:'pickup_address'},{title:'Qty',dataIndex:'parcel_quantity'},{title:'Status',dataIndex:'status',render:v=><StatusTag value={v}/>}];async function submit(values){await api.post('/merchant/pickups',values);message.success('Pickup requested');setOpen(false);setRefresh(Date.now());}return <><SimpleTablePage title="Pickup Requests" endpoint="/merchant/pickups" columns={columns} reloadKey={refresh} extra={<Button type="primary" onClick={()=>setOpen(true)}>Request Pickup</Button>}/><Modal open={open} title="Request Pickup" onCancel={()=>setOpen(false)} onOk={()=>form.submit()}><Form form={form} layout="vertical" onFinish={submit} initialValues={{parcel_quantity:1}}><Form.Item name="pickup_name" label="Pickup Name" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="pickup_phone" label="Phone" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="pickup_address" label="Address" rules={[{required:true}]}><Input.TextArea/></Form.Item><Form.Item name="pickup_city" label="City"><Input/></Form.Item><Form.Item name="pickup_area" label="Area"><Input/></Form.Item><Form.Item name="parcel_quantity" label="Parcel Quantity"><Input/></Form.Item></Form></Modal></>;}
